services:
  - type: web
    name: shipdoc-verifier
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: ANTHROPIC_API_KEY   # optional, enables the LLM second opinion
        sync: false
      - key: MISMATCH_FORMAT
        value: dict
